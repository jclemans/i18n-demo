%html
  %head
    %title Store Demo

  %body
    %h1= t ".title"
    %em= t ".description"

    %ul
      -Product.all.each do |product|
        %li= link_to product.name, product_path(product) 
        %p= "Description: " + product.description 
        %p= "Price: " + number_to_currency(product.price, precision: 2)
